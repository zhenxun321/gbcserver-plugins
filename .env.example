# 你可以不使用 .env，直接在系统环境变量里设置。
# 如果使用 .env，请自行用 dotenv 加载（本项目默认不强制依赖 dotenv）。

# 配置文件路径（可选）
# CONFIG_PATH=config.json

# 服务器令牌（用于 useCoupon 等接口）
SERVER_TOKEN=CHANGE_ME_SERVER_TOKEN

# 管理员（可选：若不设置，首次运行会自动生成并写入 admin.json）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_ME_ADMIN_PASSWORD
ADMIN_TOTPSECRET=BASE32_TOTP_SECRET

# 邮件
MAIL_HOST=smtp.example.com
MAIL_PORT=465
MAIL_SECURE=true
MAIL_USER=your_smtp_user
MAIL_PASS=your_smtp_password
MAIL_FROM="Server <noreply@example.com>"
MAIL_LOGOURL=

# TLS 证书路径
TLS_KEYPATH=server.key
TLS_CERTPATH=server.pem

# 插件 IP 白名单（JSON 数组字符串）
PLUGIN_ALLOWLIST=["127.0.0.1","::1"]

# 服务器列表（JSON 数组字符串）
SERVERS=[{"name":"示例线路1","host":"example.com","defaultPort":25565}]

# 端口
PORTS_HTTP=80
PORTS_HTTPS=443
PORTS_PLUGIN=8094
